version: '3.8'

services:
  web2fe:
    build:
      context: .
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
        - NEXT_PUBLIC_WEBSOCKET_URL=${NEXT_PUBLIC_WEBSOCKET_URL}
    container_name: web2fe
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env
